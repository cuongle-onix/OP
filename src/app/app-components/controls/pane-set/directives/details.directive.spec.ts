import { DetailsDirective } from './details.directive';

describe('DetailsDirective', () => {
  it('should create an instance', () => {
    const directive = new DetailsDirective();
    expect(directive).toBeTruthy();
  });
});
