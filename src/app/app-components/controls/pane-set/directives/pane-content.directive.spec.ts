import { PaneContentDirective } from './pane-content.directive';

describe('PaneContentDirective', () => {
  it('should create an instance', () => {
    const directive = new PaneContentDirective();
    expect(directive).toBeTruthy();
  });
});
