import { SplitBehaviourDirective } from './split-behaviour.directive';

describe('SplitBehaviourDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitBehaviourDirective();
    expect(directive).toBeTruthy();
  });
});
